# Fixtures semânticas sintéticas

As entradas versionadas do classificador ficam em
`test/contextual_classification_test.dart` e usam somente OCR e evidências
visuais fictícias. Imagens privadas de avaliação devem ser colocadas em
`test/fixtures/private_semantic/`, que é ignorado pelo Git.
